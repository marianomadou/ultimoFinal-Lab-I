#include <stdio.h>
#include <stdlib.h>
#include "control.h"
#include "lib.h"

///private functions

void control_init()
{
   /* ArrayList* letras=al_newArrayList();
    int opcion, salir=0;
    do
    {
        vista_menuPrincipal();
        switch(opcion=entero_get("opcion"))
        {
        case 1:
            letras_parseIn(letras,"datos.csv");
            break;
        case 2:
            letras_evaluate(letras);
            letras_parseOut(letras,"resultados.csv");
            break;
        case 0:
            printf("Adios!\n");
            system("pause");
            salir = 1;
            break;
        default:
            vista_opcionInvalida();
            break;
        }
    }
    while(salir==0);*/
}

///private functions
